"""
Сохранение и загрузка полного состояния симуляции.

save_state(world, path)  — сериализует World в файл
load_state(path) -> World — десериализует World из файла

Импортирует:
    simulation.world.World

Экспортирует:
    save_state
    load_state
"""
